# 100WR Safe Fast Implementation Plan

> **For agentic workers:** Execute task-by-task. Steps use checkbox syntax.

**Goal:** Add a fast 5m forever_safe + hw_range config and measure open/close activity.

**Architecture:** Config-only first; reuse existing backtest/paper/`hw_range`/`forever_safe`. Only touch code if 5m signals are cost-blocked or re-entry is gated.

**Tech Stack:** Python, yfinance, Aegis backtest/paper.

## Global Constraints
- Keep forever_safe halt-on-first-loss
- Do not modify original `config_100wr_forever_safe.yaml` behavior
- No fake “100 trades/min” claims

---

### Task 1: Config + report scaffolding

**Files:**
- Create: `bot/config_100wr_safe_fast.yaml`
- Create: `bot/reports/100WR_SAFE_FAST.md` (after run)

- [x] Write `config_100wr_safe_fast.yaml` (15m denser hw_range + forever_safe recycle)
- [x] Respect `hr_halt_on_first_loss` in `aegis/high_risk.py`
- [x] Run backtest; write `bot/reports/100WR_SAFE_FAST.md`

