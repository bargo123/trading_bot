# Rapid multi-position behavior

The deterministic basket, turnover, checkpoint, execution-audit, circuit, and
reconciliation tests passed together (224 passed in 8.58s). The test evidence
covers the existing multi-position ownership and confirmed-close behavior; it
does not claim a live latency or profitability result.

No MT5 runner restart or broker smoke was performed, so live fill/close counts
for this integration pass are `NOT_MEASURED`.
