Candidate H1: the validated-state gate may be rejecting profitable challengers because the uniform executable-overlap window truncates strategies whose edge matures later than the standard horizon. Test: replay the most recent sealed holdout challengers (bot/research/sealed_holdouts.jsonl, registry in bot/research/experiments.sqlite) using each candidate's own target horizon — now measurable thanks to the per-candidate-horizon economics already exposed in the latest research reports — and compare net-of-cost expectancy and gate verdicts against the current uniform-window run. Falsifier: the hypothesis dies if per-horizon replay flips zero previously rejected challengers to positive net expectancy, or if flipped candidates fail to survive the same spread/slippage assumptions and drawdown gates as the incumbent. Secondary check: confirm the flipped set is not concentrated in a single session or symbol (overfit-to-regime guard). This is a pure replay/analysis cycle registered in the ExperimentRegistry; no code, config, or champion changes, and no order path is touched.