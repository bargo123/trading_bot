Test whether a supported Council model can evaluate a minimum 20% post-cost expected-value improvement gate before validated-state entry. Evaluate the gate on sealed holdouts and the next 100 eligible MT5 DEMO signals, comparing net expectancy, drawdown, and abstention rate against the current gate. Adopt only if net expectancy improves with no increase in maximum drawdown; otherwise reject and record the unsupported-model failure.