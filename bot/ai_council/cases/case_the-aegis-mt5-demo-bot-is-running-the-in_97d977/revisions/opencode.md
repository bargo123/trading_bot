Test the exit refinement using an available, supported model or a deterministic rule-only evaluation path; record any unavailable-model failure without fabricating results. Evaluate sealed MT5 DEMO historical/replay episodes net of configured spread and slippage against the current champion. Accept only if holdout net expectancy improves with no increase in 95th-percentile tail loss or maximum drawdown; otherwise reject and retain the champion.