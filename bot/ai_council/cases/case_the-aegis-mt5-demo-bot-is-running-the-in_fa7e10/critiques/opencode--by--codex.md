The threshold of 30 is arbitrary and may overfit to the tested symbols/windows, while conflating sample quantity with regime quality and liquidity. “Drawdown decreases” and “expectancy improves by at least 10%” lack statistical uncertainty, power, horizon, and multiple-comparison controls, so repeated sealed-window tuning could still leak information. The proposal also omits an explicit DEMO-only/config-governance check and confirmation that the test cannot alter live execution or bypass existing risk and broker gates.