Test whether tightening the validated-state gate to require 20 consecutive completed outcome records improves net expectancy versus the current threshold on sealed holdouts. Accept it only if net expectancy improves after modeled spread/slippage while trade count remains at least 70% of baseline and worst-case drawdown does not increase. Record the experiment in `ExperimentRegistry`; otherwise reject the candidate.