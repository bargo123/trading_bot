Test whether tightening the validated-state gate to require 20 consecutive completed outcome records improves net expectancy versus the current threshold on sealed holdouts. Use a supported configured model rather than `x-preview-f-free`, and record any unavailable-model failure honestly in `ExperimentRegistry`. Accept only if net expectancy improves after modeled spread/slippage, trade count remains at least 70% of baseline, and worst-case drawdown does not increase; otherwise reject the candidate.