Proposal: Add a hard tail-loss exit rule as the next exit_research challenger. Current reconciled demo evidence shows WR=67.5% but PF=0.77 and expectancy=-0.012 because payoff geometry is broken — avg_win 0.060 vs avg_loss -0.177, with tail losses (worst -1.21) each erasing ~20 average wins, and 1,927 of 2,517 exits closing under "manual" rather than a managed tp/sl rule (tp closes alone run +0.107 expectancy). Candidate: cap per-trade loss at 0.30 (~1.7× current avg loss, half the observed tail) via a synthetic-stop replay over the existing outcome_log exits, evaluated net of runner spread/slippage on a sealed holdout. Falsifiable prediction: this flips aggregate PF above 1.0 and reduces wins_erased_by_tail_loss from ~20 to <3 while keeping win rate ≥55%; if the holdout does not show both, the challenger is rejected and recorded in the ExperimentRegistry as failed. This follows "Building Winning Algorithmic Trading Systems" (Davey), which stresses fixing trade-management geometry and Monte-Carlo/sealed validation before any strategy change; no champion or live config is touched.