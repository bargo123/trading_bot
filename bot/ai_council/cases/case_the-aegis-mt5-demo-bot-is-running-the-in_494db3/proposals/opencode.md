Test a stricter validated-state confidence calibration gate that abstains when the rolling out-of-sample calibration error exceeds a preset threshold. Compare it against the current gate on sealed MT5 DEMO outcomes, net of modeled spread and slippage. Promote it only if it reduces tail loss without reducing net expectancy or trade-count-adjusted performance beyond predefined limits.