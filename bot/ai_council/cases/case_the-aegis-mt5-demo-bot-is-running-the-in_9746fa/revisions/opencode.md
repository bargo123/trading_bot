Test a volatility-regime filter that abstains from entries when 15-minute ATR exceeds the 90th percentile of the prior 20 trading days. Evaluate it on sealed holdouts using the existing spread/slippage assumptions, comparing net expectancy, maximum drawdown, trade count, and confidence intervals against the current validated-state baseline. Use a supported, authenticated model endpoint for any AI-assisted analysis; if unavailable, run the evaluation deterministically without it. Accept only if net expectancy improves and the 95% confidence interval excludes a drawdown increase; otherwise retain the current champion.