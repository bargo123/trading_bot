Test a stricter validated-state freshness gate that rejects entries when the last validated market-state update is older than 60 seconds. Evaluate net, cost-adjusted out-of-sample expectancy, maximum drawdown, and qualified trade count against the current gate on sealed MT5 DEMO replay data. Use a supported model configuration for the evaluation pipeline, replacing the unavailable `x-preview-f-free` model. Adopt the gate only if expectancy improves without worsening drawdown or reducing qualified trades by more than 20%.