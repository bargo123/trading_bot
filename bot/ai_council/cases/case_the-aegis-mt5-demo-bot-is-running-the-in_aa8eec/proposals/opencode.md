Test whether requiring two consecutive validated-state observations before entry reduces false-positive signals. Compare it against the current single-observation gate on sealed MT5 DEMO historical slices, using identical spreads, slippage, risk limits, and no code promotion. Accept the candidate only if net expectancy improves while trade count remains at least 70% of baseline and maximum drawdown does not increase.