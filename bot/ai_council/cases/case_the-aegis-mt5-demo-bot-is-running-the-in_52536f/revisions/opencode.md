Preregister the threshold study in `ExperimentRegistry`, comparing the current threshold with justified candidate increments across sealed, non-overlapping market regimes. Evaluate leak-free returns using conservative, documented spread, slippage, and execution assumptions, reporting confidence intervals and bootstrap significance for net expectancy, drawdown, and risk-adjusted returns. Adopt a higher threshold only when its improvement is statistically supported across regimes, improves portfolio risk-adjusted performance, and does not materially worsen drawdown or capacity after costs. Treat the unsupported model error as an infrastructure issue and do not substitute model outputs or fabricate results.