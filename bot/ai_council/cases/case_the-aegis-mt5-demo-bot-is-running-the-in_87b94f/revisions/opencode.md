Revised proposal: Run the exit-geometry experiment only after two gates clear. Gate 1 (data trust): reconcile the n_survive=0 vs "2 survivors" contradiction across `validated_states.json`, strategy_selection, and `outcome_learning.json` — logged in ExperimentRegistry — before any evaluation, since the reconciliation determines which fires are even admissible. Gate 2 (identifiability): admit only exits with real open/close timestamps and per-trade MFE/MAE (or bars recoverable from MT5 between those timestamps); reconstruct each intratrade path from those bars alone and simulate the fixed ≤1×TP invalidation with adversarial fills — any bar touching the stop fills at the stop worsened by full measured spread plus the 2.0-pip p75 slippage, never better — excluding and counting unresolvable trades. Preregister in the registry before touching the holdout: one candidate only (invalidation at 1× TP distance, fixed a priori, not tuned to this sample), a strictly time-ordered untouched split (final 20% by close time) with stratified reporting by symbol and session, and a primary endpoint of net expectancy in pips/trade computed from the simulated trade set itself — with win-rate degradation estimated from the MFE distribution (trades invalidated before their eventual MFE become losses) rather than assuming 67.5% holds — so the pass/fail call comes from simulation, not a payoff-ratio identity; any extra variants are labeled exploratory under Holm correction. If net expectancy ≤0 or PF <1.0 on the holdout, the candidate is rejected and sealed; research-only throughout: no champion edit, no live YAML, no orders.