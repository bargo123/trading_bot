Revised proposal: Run an exit-rule experiment in `exit_research` testing replacement of the `quick_win` flatten rule with pre-registered hold-to-validated-target and trailing-stop variants (all parameters frozen in the registry entry *before* any evaluation), using a chronological 70/30 time-split of the 2,404 logged deals so that the in-sample replay serves only for parameter selection and the falsifiable prediction is judged exclusively on the untouched final-period holdout, net of runner spread/slippage assumptions. We withdraw the causal claim that "entries work": the cohort statistics are descriptive and confounded by state, regime, and exit-selection effects, so the experiment instead compares exit-rule counterfactuals within matched state/regime strata against the quick_win baseline rather than attributing P&L to entry quality. The Tharp and reward:risk citations are motivation only and carry no evidential weight; all support must come from the held-out replay. Falsifiable prediction: on the holdout, target/trailing exits improve net expectancy from −0.011 to > +0.01/trade while keeping 5% tail loss ≤ the current −0.49; otherwise the challenger is rejected and sealed under the full gate sequence in `bot/aegis/research/promote.py`. There is no demo A/B and no order placement of any kind — evaluation is strictly read-only counterfactual replay of logged deals plus read-only MT5 data fetches, honoring the never-trade boundary. Every run is registered in `ExperimentRegistry` with pinned seeds, code commit hash, split boundaries, and frozen parameters; pipeline failures or null results are recorded honestly as failed experiments, with no champion or live YAML changes under any outcome.