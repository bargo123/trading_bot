Test a stricter validated-state entry filter requiring at least 30 completed trades for the active symbol/session regime and a positive 95% bootstrap lower confidence bound on net expectancy after modeled spread and slippage. Compare it with the current gate on a sealed holdout using identical signal timestamps, position sizing, and exits. Accept it only if holdout net expectancy improves by at least 10% without increasing maximum drawdown or reducing trade count by more than 25%; otherwise reject and record the result.