Test one pre-registered, research-only two-confirmation rule whose signals come from disjoint feature families and have no shared label-derived inputs, with independence defined before evaluation by a fixed residual-correlation threshold. Define baseline expectancy as the existing validated-state gate’s cost-adjusted per-trade expectancy, using identical spread, slippage, entry, and exit assumptions on the same sealed holdout. Require at least 200 holdout trades, regime-stratified results with pre-specified minimum counts, and a 95% bootstrap confidence interval whose lower bound for relative expectancy improvement is at least 10%, while maximum drawdown is no worse than baseline. If candidate pairs are screened, selection must occur only on a separate development set with a pre-registered multiplicity correction; otherwise evaluate the single sealed pair, reject it if any gate fails, and change no champion or live configuration.