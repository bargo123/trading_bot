Evaluate the challenger once on a fresh, immutable, single-use sealed holdout, with the current champion run over the same quote stream and evaluation window; require at least 200 completed trades per arm, and define invalidation/scratch exits using immutable OMS reason codes, reporting both rates and absolute counts. The challenger passes only if its invalidation/scratch rate is at least 15% lower, with a pre-specified 95% confidence interval supporting that threshold, while remaining non-inferior on total net P&L, expectancy per trade, trade frequency, drawdown, tail loss, exposure duration, and pre-specified regime results; power, margins, and multiple-testing correction must be fixed before unsealing, and the holdout cannot be reused. After the second confirming completed bar, entry occurs at the executable open of the next bar, with spread and slippage charged at that execution and no data from that bar used in the decision. Any pass must still be logged in ExperimentRegistry and submitted to every gate in `bot/aegis/research/promote.py`; otherwise, reject it and retain the current champion.