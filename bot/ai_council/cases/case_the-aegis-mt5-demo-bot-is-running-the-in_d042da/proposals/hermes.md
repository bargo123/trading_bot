Candidate improvement: replace the validated-state gate's static validation window with a rolling walk-forward revalidation aligned to each candidate's own horizon, evaluated strictly as an offline replay over existing records (bot/research/sealed_holdouts.jsonl, ExperimentRegistry, outcome logs) before any champion change. Hypothesis: a meaningful fraction of gate rejections are candidates whose edge decays within days rather than being absent, so a decay-aware recheck would raise net-of-cost expectancy per accepted trade without raising tail risk. Falsifiable test: replay both the current static gate and the rolling gate over the same sealed slices, comparing net expectancy after runner spread/slippage assumptions, acceptance rate, maximum drawdown, and the share of accepted trades invalidated inside their own horizon. Prediction: the rolling gate beats the static gate by at least ~10% net expectancy per accepted trade at equal-or-lower drawdown; if it fails on any sealed slice, register the failure honestly in the experiment registry and leave the champion untouched.