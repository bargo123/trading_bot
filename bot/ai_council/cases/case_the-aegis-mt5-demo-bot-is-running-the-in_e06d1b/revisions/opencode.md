Pre-register the filter in ExperimentRegistry: positive EV uses a fixed, training-derived threshold, and spread percentile uses only a causal rolling historical window, with neither recalibrated after holdout access. Run it once on the sealed holdout versus the current gate under identical costs, using paired trade-outcome analysis with a pre-specified minimum sample size, minimum net-expectancy improvement, 80% power target, and positive 95% OOS lower confidence bound. Report results by session and volatility regime; credit improvement only if it persists beyond low-volatility-session selection. Advance only through all `promote.py` gates; otherwise record and reject the challenger without further sealed-holdout tuning. Any enabled paper path remains MT5 DEMO-only with `allow_live: false`, fail-closed account checks, and the sole execution owner `run_broker_paper.py`.