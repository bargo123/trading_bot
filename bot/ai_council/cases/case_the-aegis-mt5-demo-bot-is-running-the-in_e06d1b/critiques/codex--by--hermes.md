Selecting the winner directly on the sealed chronological holdout unseals it — the holdout becomes part of your selection loop and loses its purpose; validate on walk-forward/OOS folds first and spend the holdout only as a single final confirmation, recorded in the ExperimentRegistry per house rules. The acceptance criteria carry no statistical machinery: a 10% expectancy gain with a 5% drawdown tolerance is meaningless without confidence bounds or a permutation/bootstrap test, and a stricter persistence gate will slash trade counts, so the comparison will likely rest on too few trades to distinguish signal from noise. The gate parameters (two consecutive bars, 1.25×, 20-bar window) appear hand-picked with no pre-registration or sensitivity analysis — three free thresholds tuned against one outcome metric is textbook overfitting surface. Finally, the proposal never states the rolling spread median must be strictly trailing (lookahead risk), leaves the expectancy/drawdown baselines undefined (paired same-period comparison vs. what), and frames acceptance as its own decision when promotion must pass every gate in promote.py through the governed script, not an ad hoc two-metric rule.