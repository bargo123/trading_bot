Four weeks may yield too few rejected signals for a reliable bootstrap interval, especially across correlated market regimes; no minimum sample size or independence treatment is specified. “One average spread unit” is an arbitrary, unstable benchmark unless spread measurement and aggregation are predefined. The counterfactual must model fillability, latency, position limits, and signal overlap, or it will overstate discarded edge despite claiming paper-path economics.