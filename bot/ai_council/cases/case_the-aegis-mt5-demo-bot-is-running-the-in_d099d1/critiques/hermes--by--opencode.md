The pass criterion ignores net expectancy and tail loss, so it could reject profitable opportunities merely to cosmetically shorten streaks. A 20% reduction in mean streak length is vulnerable to small samples and regime-specific overfitting; require sealed temporal holdouts, confidence intervals, and comparison against randomized cooldown baselines. “Same validation level” and “confirmation bar” need precise, timestamped definitions to make the replay reproducible.