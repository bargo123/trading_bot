Candidate: add a post-invalidation cooldown to the validated-state gate. Hypothesis: losses cluster because the gate currently re-arms immediately at the same validation level that just failed, so requiring either a short cooldown (e.g., 15 minutes) or one confirmation bar before re-arm will break losing streaks without meaningfully reducing opportunity set. Falsifiable test: offline replay of the existing DEMO outcome/fill logs through both gate variants (no code deployed, no orders), comparing net-of-spread expectancy per trade, maximum consecutive-loss streak, and total qualifying-trade count. Pass criterion: the cooldown variant cuts mean losing-streak length by at least 20% with under 10% fewer qualifying trades; any other result is recorded as a failure in ExperimentRegistry and the champion stays untouched.