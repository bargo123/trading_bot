Candidate: add a post-invalidation re-arm condition to the validated-state gate — after a confirmed close-out at a loss, re-arming requires either a 15-minute cooldown measured from the closing fill's server timestamp, or one fully completed strategy-timeframe bar whose close timestamp is strictly later than that fill; "same validation level" is defined as the exact recorded entry price/ticket of the failed attempt in the DEMO outcome and fill logs, making every replayed re-arm decision deterministic and timestamp-reproducible. Test: an offline, code-free event-driven counterfactual replay over purged and embargoed walk-forward folds of the historical DEMO logs, with the final fold held out as a sealed temporal holdout, comparing gate-off control against each variant against a placebo baseline of randomized cooldown lengths. Pass criteria, all evaluated on the sealed holdout with 95% block-bootstrap confidence intervals: net-of-spread expectancy per qualifying trade degrades by no more than 5% versus control, 95th-percentile per-trade loss does not worsen, qualifying-trade count falls by less than 10%, and mean maximum consecutive-loss streak shortens by at least 20% with the improvement significant at α = 0.05 against both control and the randomized-cooldown distribution; any other result is recorded as a failure in ExperimentRegistry and the champion remains untouched. If and only if promoted, deployment adds this condition solely as an additional precondition inside the existing validated-state check of the single governed MT5 DEMO executor path (`bot/scripts/run_broker_paper.py`), leaving every quote, spread, EV, risk, OMS, total-drawdown, and close-confirmation gate unchanged and all Council/Research components read-only.