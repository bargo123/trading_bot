Test whether requiring a second consecutive validated-state confirmation before MT5 DEMO entry reduces false entries net of modeled spread and slippage. Use a supported, authenticated model endpoint for any AI-assisted analysis; if unavailable, run the deterministic evaluation without it and record the limitation. Compare against the current gate on sealed holdouts, recording trade count, net expectancy, drawdown, missed-opportunity rate, and execution status in the experiment registry. Reject the candidate unless net expectancy improves without worsening total drawdown or reducing qualified trades by more than 25%.