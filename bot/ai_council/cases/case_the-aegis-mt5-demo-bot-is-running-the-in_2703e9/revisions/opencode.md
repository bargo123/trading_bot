Run the sealed-holdout experiment using a supported configured model, with a deterministic local fallback if model access fails. Add a two-consecutive-scan validated-state persistence requirement before entry, then compare net expectancy after spread/slippage, maximum drawdown, and trade count against the current gate over the same MT5 DEMO outcome window. Reject the candidate unless it improves net expectancy without increasing maximum drawdown or reducing qualifying trades by more than 20%, and record any model-access failure in the experiment registry.