Test a regime-conditioned admission threshold for validated firehose signals, using volatility and session as pre-entry features while preserving all existing gates. Register a costs-netted walk-forward ablation against the current champion and evaluate it on the sealed holdout. Accept the candidate only if it improves net expectancy by at least 10% without increasing drawdown or invalidating calibration; otherwise reject it.