The acceptance rule is under-specified and overfit-prone: the regime definition, volatility bucketing, and the magic 10% expectancy bar are all researcher-tuned, yet you propose a single sealed-holdout peek with no power analysis or multiple-comparison control—on typical firehose per-regime sample sizes, a 10% net-expectancy delta will be statistically indistinguishable from noise, and conditioning itself fragments the sample into thin regimes that flatter the result. "Without invalidating calibration" is unmeasurable as written—name the calibration metric, its tolerated degradation, and which drawdown statistic (max DD? time-under-water?) binds—or the clause is gate theater. Missing validation details: fold count, purge/embargo between walk-forward segments, and proof that regime labels persist out-of-sample rather than reshuffling at the boundary. Finally, commit that the holdout is unsealed exactly once and only after every promote.py gate passes; any iterative re-evaluation against it converts your seal into a second training set.