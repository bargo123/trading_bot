Test a pre-registered Asia-session sell filter using a 20-bar trailing ATR and trailing 100-bar median ATR-normalized spread, computed only from bars available at entry. Require at least 100 net-of-cost trades across three chronologically separated sealed holdouts and walk-forward folds; accept only if pooled expectancy improves by at least 0.05R with a one-sided bootstrap 95% lower bound above zero, maximum drawdown is no worse than the champion by 5%, and each regime has non-negative expectancy. Lock all parameters before evaluation, adjust significance using Benjamini-Hochberg across tested variants, and stress costs at 1.0x, 1.5x, and 2.0x configured spread/slippage. Record every variant and result in the experiment registry, retain all candidates unpromoted unless every governed promotion gate passes, and preserve failed candidates and holdout results for audit.