Proposal (revised): The reconciled demo exit log (outcome_learning.json, n=2,517) shows win_rate 67.5% against payoff_ratio 0.34 (SL closes −0.547 vs TP +0.107; aggregate expectancy −0.012, PF 0.77); we recast "loss geometry, not signal quality" from a conclusion into a competing-hypotheses design — a preregistered experiment replays closed positions counterfactually through exit_research under three fixed arms (SL at 1.0× and 0.5× current distance plus a 24-bar time-stop) against a randomized-entry control, since close-log replay cannot recover unobserved intrabar fills and stop mechanics are therefore modeled adversarially (any bar touching SL fills beyond it) net of runner spread, slippage, and gap handling with strictly point-in-time features. All variants are entered in ExperimentRegistry before any metric is computed; walk-forward folds are temporally purged with an embargo buffer longer than the label horizon, and the ≥200-trade holdout is evaluated exactly once, at final validation. The pass gate replaces the arbitrary win-rate floor with statistically credible criteria: holdout net expectancy whose bootstrap 95% CI excludes zero under a permutation null, PF with CI lower bound above 1.0, max drawdown and CVaR no worse than baseline beyond a pre-stated tolerance, and expectancy-sign stability across at least three regime partitions. If no arm clears every gate, the rejection is recorded in the registry and the challenger sealed — no champion change, no live config touched. The book-memory observation (9 of 108 notes defining an invalidation level) is retained purely as motivation for the hypothesis, not as evidence.