Pre-register a small threshold grid using training data, then select once via purged chronological walk-forward validation across multiple MT5 DEMO regimes using conservative cost haircuts above recorded spread and slippage. Evaluate the locked candidate once on a fresh sealed DEMO window against the champion, requiring a minimum trade and power floor, higher aggregate net PnL and expectancy with confidence intervals, and no material CVaR(5%) regression under a pre-specified equivalence margin and one-sided test. Correct for the threshold search and any prior challenger evaluations, and treat insufficient evidence as rejection rather than no regression. The filter must use out-of-sample calibrated edge estimates, remain abstention-only, and cannot modify governed risk, execution ownership, DEMO-only checks, or any broker execution gate.