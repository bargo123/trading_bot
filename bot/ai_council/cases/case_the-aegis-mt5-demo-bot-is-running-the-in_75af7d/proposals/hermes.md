Proposal: Now that split primary OOS economics and sealed multi-outcome metrics are exposed in the research reports, test whether promotion should require positive net-of-spread expected value in each OOS split independently rather than only on the pooled OOS window. Concretely, next cycle should recompute every recently promoted challenger plus the current champion from `bot/research/sealed_holdouts.jsonl` under a strict per-split gate (positive net EV in both splits, no offsetting between regimes), recorded via ExperimentRegistry. The hypothesis is falsifiable: if at least one historically promoted strategy fails the per-split check while having passed the pooled gate, the pooled criterion is masking regime fragility and the gate change is justified; if all pass both splits cleanly, the stricter gate adds no discrimination and should be discarded. No code or champion change occurs until this retrospective passes and a follow-up prospective challenger set confirms it through the standard `promote.py` gates.