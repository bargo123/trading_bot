Run one pre-registered, one-shot ablation after tuning quote-age and spread thresholds only on a separate validation split, using strictly trailing, instrument-specific 30-day medians computed from information available at each replay timestamp. Define validated state and replay fidelity explicitly, including quote timestamp-quality filters, deterministic spread/slippage assumptions, and conservative missed-fill handling. Evaluate walk-forward across instruments and distinct volatility, rollover, and news regimes before touching the sealed, time-ordered DEMO holdout. Compare aggregate net PnL, exposure-adjusted expectancy, trade count, and maximum drawdown using a paired bootstrap over matched trade timestamps, with promotion requiring a pre-specified positive PnL improvement and bootstrap confidence bound above zero. Define material tail deterioration as CVaR95 or maximum drawdown exceeding baseline by more than 10%, with no promotion if that limit is breached or if gains arise solely from reduced exposure.