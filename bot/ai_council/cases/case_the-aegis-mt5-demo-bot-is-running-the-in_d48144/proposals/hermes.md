Candidate improvement: convert the validated-state gate from a fixed-threshold accept/reject into a calibrated abstention-margin rule — require the validator's posterior margin between its top-1 and top-2 regime/state hypotheses to exceed a cutoff derived from historical calibration data before authorizing entry, and abstain otherwise. Falsifiable test: replay the firehose's logged decision stream through the offline evaluation harness, sweep the margin cutoff, and compare net-of-spread/slippage expectancy and tail-loss (max drawdown) against the incumbent binary gate on the sealed holdout set. Adoption criterion: promote the margin rule only if it improves net expectancy with no worse drawdown at 95% bootstrap confidence; otherwise record the rejection in ExperimentRegistry and keep the champion unchanged. This targets the known failure mode where marginal-confidence states pass a binary gate yet contribute negative expected value after costs. No direct corpus passage matched this question, so the proposal rests on the system's own logged evidence rather than a cited book.