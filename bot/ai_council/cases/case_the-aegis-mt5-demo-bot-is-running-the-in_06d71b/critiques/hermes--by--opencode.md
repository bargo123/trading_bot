The threshold is internally inconsistent: the hypothesis is rejected unless at least one third fail, but the proposed gate is adopted if even one fails, enabling a post-hoc policy change. Scaling each strategy's own historical costs may preserve optimistic or stale cost estimates rather than test realistic execution conditions; no out-of-sample or market-regime validation is specified. Re-evaluating already-promoted strategies also risks retrospective churn without a governed demotion policy or confirmation that `promote.py` can safely enforce the new criterion.