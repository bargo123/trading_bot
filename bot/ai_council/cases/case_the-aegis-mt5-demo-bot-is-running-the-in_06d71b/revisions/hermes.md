Revised proposal: add a cost-stress revalidation study to the next research cycle, run read-only over existing records (sealed holdouts, the sitting champion spec, logged outcomes, and also the non-promoted challenger specs already kept in bot/research/strategies/, so inference is not restricted to promoted survivors). First establish faithfulness: rescore each record through the unchanged backtest at 1.0x and require it to reproduce the recorded net expectancy within a pre-registered tolerance, excluding and honestly logging any record that cannot be reconstructed before any multiplier analysis. Then rescore surviving records at 1.25x and 1.5x using the runner's current configured spread/slippage assumptions applied uniformly — not each strategy's own historical estimates — recording net expectancy and max drawdown per multiplier in ExperimentRegistry as a single pre-registered test (no other gate criteria ride on this cycle). Decision rule made internally consistent: reject the hypothesis if zero reconstructed records show non-positive net expectancy even at 1.5x; if any do, compute the exact binomial lower confidence bound on the failure rate and adopt "positive net expectancy at 1.25x" as a promote.py gate condition only if that bound exceeds 10% at 95% confidence, with drawdown acceptance defined simultaneously as max drawdown at 1.25x not exceeding 1.5x the strategy's recorded value. Any consequence for already-promoted strategies flows exclusively through the governed promoter path (research_promote_champion.py) in the normal cycle — flagged champions are marked cost-fragile on record and re-challenged, never directly demoted — and the new gate ships only after a passing promote.py regression test demonstrates it can enforce the criterion without breaking existing promotions.