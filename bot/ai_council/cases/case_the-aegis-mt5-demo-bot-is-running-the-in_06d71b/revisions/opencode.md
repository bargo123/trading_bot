Pre-register and log in `ExperimentRegistry` a single-shot challenger using a 1.25x threshold and trailing 60-minute tick-sampled median spread excluding the current quote, with explicit stale/sparse-quote handling; freeze all parameters before first sealed-holdout access. Evaluate versus the unchanged validated-state baseline across sealed out-of-time holdouts and predefined symbol/session/news regimes, confirming spread-distribution relevance and composition with existing quote and economics gates under conservative, independently validated spread/slippage assumptions. Define net expectancy as net PnL per executed trade, require a minimum pre-registered trade count, a bootstrap 95% confidence interval for the expectancy delta above zero, at least 10% relative improvement, non-degraded total net PnL, and maximum-drawdown change within a pre-registered sampling-noise tolerance. Any failure retains the baseline with results recorded; a passing challenger proceeds only through `research_promote_champion.py` and every `promote.py` gate, with no post-holdout retuning.