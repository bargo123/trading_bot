Pre-register the exact spread threshold from the runner configuration and a three-sample persistence rule using one-second broker-quote observations, with stale or tick-bursty samples rejected and all three distinct samples strictly preceding the signal timestamp; select no parameters from the sealed holdout, using only training data and purged cross-validation. Register the hypothesis, data version, quote source, cadence, cost model, parameter freeze, and regime partitions in ExperimentRegistry before testing, and define net expectancy as realized P&L after the runner’s configured spread and slippage costs. Require at least 100 closed trades, a minimum 10% improvement in net expectancy with a one-sided 95% confidence interval excluding zero, at least a 5% reduction in maximum drawdown, unchanged trade-quality gates, and consistent results across the pre-registered regimes and runner-cost sensitivity checks, with Holm–Bonferroni correction across all registered champion challenges. Promote only through the explicit numeric gates in `bot/aegis/research/promote.py`; otherwise retain the challenger and sealed-holdout results without promotion.