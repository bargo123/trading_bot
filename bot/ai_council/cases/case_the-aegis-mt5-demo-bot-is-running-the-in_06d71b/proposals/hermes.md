Candidate improvement: add a cost-stress revalidation step to the validated-state gate for the next research cycle. Hypothesis: the current gate can promote challengers whose net edge after the runner's assumed spread/slippage is small enough that realistic cost variation flips them negative, meaning some promotions are cost-noise artifacts rather than durable edges. Test: using only existing records (sealed holdouts in bot/research/sealed_holdouts.jsonl, the sitting champion spec, and logged outcomes), rescore every previously promoted strategy through the unchanged backtest at 1.0x, 1.25x, and 1.5x of its own measured historical spread/slippage assumptions, logging net expectancy and max drawdown per multiplier in ExperimentRegistry — no code, config, or champion changes during measurement. Falsifiable decision rule: if fewer than one third of promoted strategies show non-positive net expectancy at 1.25x costs, reject the hypothesis and leave the gate unchanged; if even one does, adopt "positive net expectancy at 1.25x costs" as an additional promote.py gate condition and re-evaluate the affected strategies on record. This is fully read-only research on already-recorded data, keeps all execution authority untouched, and directly serves the standing rule that outcomes must be robust to the runner's cost assumptions.