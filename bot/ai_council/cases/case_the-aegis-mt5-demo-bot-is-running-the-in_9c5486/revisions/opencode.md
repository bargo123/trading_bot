Test whether raising the validated-state minimum sample threshold reduces false-positive entries without degrading net expectancy after spread and slippage. Run the sealed-holdout evaluation using a supported authenticated model or a model-independent local analysis path, recording any unavailable-model failure honestly in the experiment registry. Compare trade count, net expectancy, maximum drawdown, and confidence calibration against the current gate. Accept only if net expectancy improves and drawdown does not increase; otherwise retain the current threshold.