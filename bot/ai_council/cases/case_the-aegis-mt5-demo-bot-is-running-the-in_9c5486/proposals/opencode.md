Test whether raising the validated-state minimum sample threshold reduces false-positive trade entries without degrading net expectancy after spread and slippage. Evaluate it on sealed historical holdouts against the current gate, recording trade count, net expectancy, maximum drawdown, and confidence calibration. Accept only if net expectancy improves and drawdown does not increase; otherwise retain the current threshold.