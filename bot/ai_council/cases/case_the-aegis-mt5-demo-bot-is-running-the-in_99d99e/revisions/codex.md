Research a pre-registered, cost-aware loss-tail exit policy with the adverse threshold fixed before evaluation, preserving existing take-profit logic and validated-state gating. Evaluate it through paired counterfactual replay against the current SL/manual policy across multiple untouched time regimes and instruments, using the runner’s spread/slippage assumptions, a defined holding horizon, net per-trade expectancy, profit factor, maximum drawdown, and tail-loss quantiles. The candidate passes only if out-of-sample net expectancy exceeds zero and profit factor exceeds 1.0 with confidence bounds excluding the current-policy baseline, while showing no unacceptable drawdown or tail-risk deterioration under cost-stress scenarios. The result remains falsifiable because the existing 2,611-exit sample provides the baseline (PF 0.76; average losses nearly three times average wins), but no challenger may alter or replace the champion absent all promotion gates.