In the next research cycle, run an exit-research challenger (via ExperimentRegistry + sealed holdout, champion unchanged) whose cap is not tuned on the full reconciled sample: split the 2,518 exits chronologically, select the best of three preregistered caps {0.10, 0.12, 0.15} on the earlier ~70% training slice only, freeze it, and model execution pessimistically — fill at cap plus measured median demo slippage and a gap penalty for high-volatility bars — so the estimate already absorbs gaps, latency, and spread rather than assuming frictionless stops. Size the sealed holdout via a preregistered power analysis (80% power to detect a +0.02 net-of-cost expectancy shift at α=0.05, hence ≥500 exits or more as required), report bootstrap 95% CIs stratified by regime (trend/range/high-volatility), and declare success only if the overall CI excludes zero, wins_erased_by_tail_loss falls below 5, and win_rate holds ≥0.60 in every regime bucket. For the firehose-skip question, drop the causal "binding constraint" claim and replace it with a prospective audit: from cycle T0, freeze every skip's state-cluster ID, feature vector, timestamp, and analogue assignments before any outcome exists, then — only after those hypothetical trades mature — score skipped clusters against a matched contemporaneous control set of validated-state trades (TP-class baseline +0.107, n=447), controlling selection bias through like-for-like period and cluster matching. If either arm fails its preregistered criterion on the holdout, the rejection is recorded and the champion remains unchanged.