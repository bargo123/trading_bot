Replaying the already-sealed holdout to select acceptance thresholds guarantees this is its last clean use—every future challenger compared against the same burned sample compounds selection bias, so the 10% expectancy gain is indistinguishable from multiple-comparison noise without a fresh out-of-sample segment, bootstrap CIs, and a minimum eligible-signal count. "Two consecutive qualifying evaluations" and "qualifying" are undefined, and max-drawdown non-worsening on a single replay path is statistically meaningless at these sample sizes. Most seriously, "accept the candidate" via bespoke thresholds sidesteps the governed promotion gates—the council cannot self-declare acceptance criteria outside `promote.py`; the correct outcome here is "eligible for gated evaluation," nothing more. Safety hygiene (no live config edits, registry logging, no champion mutation) is otherwise compliant.