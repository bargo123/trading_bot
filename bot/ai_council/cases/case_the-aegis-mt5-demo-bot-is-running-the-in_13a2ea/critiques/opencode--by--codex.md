The fixed ~0.12 cap is tuned directly from the same reconciled sample it claims to explain, creating threshold-selection overfitting; it also assumes losses can be capped there despite gaps, slippage, and execution latency. A ≥500-exit holdout lacks a preregistered power analysis, cost model, regime stratification, and confidence bounds, while “binding constraint” is unmeasurable without a counterfactual policy and selection-bias controls for skipped states. Logging hypothetical TP-class outcomes for rejected states risks lookahead and post-selection bias unless outcomes, analogue assignment, and timestamps are frozen prospectively.