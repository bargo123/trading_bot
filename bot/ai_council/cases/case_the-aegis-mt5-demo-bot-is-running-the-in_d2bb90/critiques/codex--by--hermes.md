The two "consecutive validation windows" are almost certainly regime-correlated, so passing both is closer to one in-sample confirmation than genuine replication, and nothing states the policy's spread/volatility parameters were frozen before the holdout was sealed — without that, the 10% expectancy gain is a tuning artifact. The thresholds are unmeasurable as written: no minimum trade count, significance test, or confidence interval on expectancy (a 10% relative gain is indistinguishable from zero at typical DEMO sample sizes), and "drawdown worsens by more than 5%" is undefined — peak-to-trough on what horizon, measured against which spread/slippage assumption? The proposal is silent on multiple testing: if this is the Nth exit variant evaluated against the same sealed holdout, the gates need a deflated or multiple-comparison-adjusted criterion, otherwise repeated probing breaks the seal de facto. Finally, it must state explicitly that the 10%/5% criteria are strictly additive to every gate in bot/aegis/research/promote.py — including registry recording of rejected challengers — rather than an alternative threshold set that can be cherry-picked post hoc.