“False activations,” net expectancy, and coverage are undefined, inviting post-hoc metric selection. A single sealed holdout is insufficient for a persistence rule prone to regime-specific overfitting; require pre-registered thresholds, confidence intervals, and rolling or multiple out-of-sample validations. The proposal also omits sample-size and drawdown/tail-risk gates, so a marginal expectancy improvement could mask an unacceptable risk profile.