A single sealed-holdout comparison can still overfit if exit parameters were selected after inspecting it, and no untouched confirmation set or multiple-testing correction is specified. The 0.05R threshold and “lower 95% bootstrap bound” are undefined without sample size, bootstrap method, dependence handling, and minimum trade count; profit factor can be unstable and misleading. Acceptance also omits required promotion gates and explicit ExperimentRegistry recording, so it is not sufficient to authorize champion promotion.