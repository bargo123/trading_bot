Test a research-only, cost-aware early-loss exit using a pre-specified sample period, instrument universe, trade-count coverage, and the runner’s validated spread/slippage assumptions; report whether the 122 stop-loss exits (−70.18 net units) are representative rather than treating them as sufficient evidence of a tail-loss opportunity. Pre-register the policy and tune it only on training data, then compare it with the current exit policy on a sealed holdout with no lookahead, requiring a minimum holdout trade count and reporting bootstrap confidence intervals for expectancy, profit factor, and maximum drawdown. Account for all prior and concurrent exit-policy candidates through a documented family-wise or false-discovery multiple-testing adjustment. Define maximum drawdown as the largest peak-to-trough decline in the chronological, cost-net cumulative-equity curve, and require the candidate to pass the repository’s explicitly recorded validated-state gate— including cost inclusion, feature/label separation, sufficient sample coverage, and research-only configuration—before considering promotion.