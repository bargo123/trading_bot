Test a stricter validated-state entry rule requiring two consecutive qualified snapshots with the same directional signal before a DEMO entry is eligible. Compare it against the current gate on sealed holdouts net of configured spread and slippage, measuring trade count, net expectancy, drawdown, and invalidation rate. Accept it only if net expectancy improves without increasing maximum drawdown or reducing eligible trades by more than 25%; otherwise reject it.