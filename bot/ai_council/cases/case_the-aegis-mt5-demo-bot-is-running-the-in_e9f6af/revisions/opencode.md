Test a validated-state variant requiring two consecutive spread-normalized favorable quote updates before entry. Evaluate it against the current gate on sealed holdouts under identical DEMO cost assumptions using a supported, authenticated model configuration. Adopt only if net expectancy improves without increasing maximum drawdown or reducing eligible trade count by more than 20%.