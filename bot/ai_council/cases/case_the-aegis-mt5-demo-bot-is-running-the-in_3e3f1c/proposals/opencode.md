**Proposal — reconcile the "manual"-exit bucket before any new signal work.** The reconciled-exit evidence (`outcome_learning.json`, n=2,518) shows 76% of all exits are labeled `manual` (n=1,928, expectancy −0.013) while true `tp` exits are strongly positive (n=447, exp +0.107) and `sl` exits are rare but ruinous (n=96, exp −0.547; one 1.21 tail loss erases ~20 wins), so the aggregate −0.012 expectancy / 0.77 profit factor is dominated by an unexplained close category rather than entry quality. Next cycle should run one registered experiment that joins every `manual`-closed demo ticket back to the MT5 deal history and the runner's own exit-intent log, testing the hypothesis that most `manual` labels are actually strategy-driven (time/max-age) exits mis-attributed at journaling. Falsifiable prediction: if reclassification shows ≥60% of `manual` closes were bot-initiated, corrected expectancy turns measurably positive (>0) and the champion's true edge estimate changes sign; if instead they are genuinely external closes, the same join will show missing hard stops on the tail-loss tickets (avg loss 3× avg win), justifying a stop-placement candidate instead. No corpus passage matched directly (checked the firehose book synthesis), so this stands purely on measured data. No code or champion changes proposed here; output goes to `bot/reports/research/` and the experiment registry only.