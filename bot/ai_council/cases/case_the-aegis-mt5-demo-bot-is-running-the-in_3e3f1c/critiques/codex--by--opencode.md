The headline claim—"reduces false state transitions"—is unmeasurable as stated because nothing defines ground truth for a "false" transition; you can only measure downstream outcomes, so the proposed mechanism is unfalsifiable. Going straight to a sealed holdout with a binary accept/reject burns your only clean data on a point estimate: "net expectancy improves" carries no minimum effect size or significance test, and since a two-bar confirmation delays entries, the trade population shifts—so expectancy differences conflate gating quality with timing luck. The 70% retention floor is arbitrary and unjustified, and nothing mentions ExperimentRegistry logging or how many gate variants precede this; if this is challenge N against one holdout, it's textbook multiple-testing leakage. Finally, "accept" must mean routing through every gate in `promote.py`—the proposal's private acceptance criteria don't constitute governed promotion.