The “validated states” and exit subsets may be selected from the same `outcome_log.jsonl`, creating severe selection and lookahead risk; no independent time-split, embargoed holdout, or untouched replication set is specified. “Roughly unchanged trade count,” “existing take-profit target logic,” and “cost adjustment” are not operationally defined, while bootstrap p05 > 0 ignores multiple testing and dependence among fires. The proposal also does not require reconciliation against partial fills, missing/censored outcomes, regime drift, or a pre-registered minimum effect size, so the reported aggregate lift could be an overfit artifact despite the stated promotion safeguards.