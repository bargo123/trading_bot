Test a volatility-normalized cost filter using a threshold pre-registered at 25% of predicted edge; any alternative threshold must be selected solely on training folds and frozen before evaluation. Define predicted edge and spread/slippage estimates explicitly, freeze their implementations, and verify calibration before measuring net expectancy. Record the ablation in `ExperimentRegistry`, evaluate it once on the sealed holdout with regime- and symbol-level results, and report sensitivity across a pre-specified threshold range. Leave the champion unchanged and advance the candidate only if aggregate net expectancy improves by at least 10%, drawdown is no worse overall and by subgroup, and all existing validation gates pass.