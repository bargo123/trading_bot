Candidate: test a research-only two-consecutive-closed-bar confirmation after the validated-state gate, with the specification and thresholds pre-registered in `bot/research/strategies/` before any sealed-holdout access. Use a five-fold anchored walk-forward with a one-bar purge/embargo, and model the confirmation as delaying the fill by one complete bar; accept only on a single, non-reversible unseal if a pre-specified paired bootstrap 95% lower bound shows at least 10% improvement in cost-adjusted per-trade expectancy, trade count is at least 80% of baseline, normalized total net-of-runner-cost PnL is no lower than baseline, and maximum drawdown is no more than 5% higher than baseline measured relatively on the same equity basis. The holdout is strictly one-shot: a failure cannot be re-tuned, re-evaluated, or resubmitted, and both acceptance and rejection, including the full ablation outcome, must be recorded in `ExperimentRegistry` and retained without modifying the champion.