Proposal: Run one pre-registered, offline counterfactual exit experiment (via `bot/aegis/research/exit_research.py` + ExperimentRegistry) whose estimand is the paired within-trade policy effect: every trade in the identical population is deterministically replayed under both the incumbent `quick_win`/`never_green` flatten and each challenger (hold-to-original-TP/SL; breakeven-stop-after-+1R-MFE), so entry-selection bias cancels because both arms condition on the same stored M1 paths — we claim only "what this population would have earned under a different exit rule," explicitly disclaiming any causal statement about why manual flattens were chosen. Before any evaluation, everything tunable is frozen and hash-committed to ExperimentRegistry: the variant grid, conservative intrabar ordering (stop-before-target), per-runner spread/slippage costs, the single primary endpoint (paired net-expectancy delta), decision thresholds anchored economically to the runner's own risk budget (challenger tail_loss_5pct may be no worse than the incumbent champion's measured value, replacing the arbitrary −$0.60 floor), a power analysis sizing n from observed per-exit variance to detect Δ expectancy ≥ 0.03, a trade-level paired (clustered-by-trade) bootstrap, and Holm correction across the ≤3 variants; the sealed holdout (`bot/research/sealed_holdouts.jsonl`) is then unsealed exactly once, drawing a fresh untouched window if prior iterations ever accessed it. An automated audit enforces validity: replay inputs are restricted to pre-entry features plus the post-entry price path (no labels, outcome fields, or bucket assignments), and pre/post checksums assert `intel/intelligent_champion.json`, live runner configs, and `PROTECTED_LIVE_YAML` remain byte-identical, failing loudly otherwise. Falsifiable prediction: at the power-derived n≥500 holdout exits, challenger-minus-incumbent net expectancy > 0 with trade-clustered bootstrap 95% CI excluding zero and PF > 1.0, with tail_loss_5pct no worse than the incumbent's measured baseline; either failure rejects "the flatten rule destroys a real gross edge" and is recorded honestly. No code, config, YAML, or champion changes — any surviving variant still faces every `promote.py` gate before promotion could even be considered.