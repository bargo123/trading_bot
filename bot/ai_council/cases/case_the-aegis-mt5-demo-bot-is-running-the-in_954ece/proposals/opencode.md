Proposal: Run one pre-registered, offline counterfactual exit experiment (via `bot/aegis/research/exit_research.py` + ExperimentRegistry) testing whether replacing the `quick_win`/`never_green` early-flatten with hold-to-original-TP/SL plus a breakeven-stop-after-+1R-MFE variant flips net expectancy positive on the identical trade population. Motivating measurement: latest `outcome_learning.md` (n=2518) shows WR 0.675 yet expectancy −0.0122 and PF 0.77 because payoff ratio is 0.34 — the manual/flatten bucket (n=1928) earns −0.013 while broker-hit TP closes earn +0.107 and SL closes cost −0.547, so the exit policy, not entry selection, nets away the edge (the exit-asymmetry principle of *Trade Your Way to Financial Freedom*). Replay uses stored M1 paths with conservative intrabar ordering (stop-before-target), costs applied per the runner's spread/slippage assumptions, tuned in-sample then frozen and judged on the sealed holdout (`bot/research/sealed_holdouts.jsonl`). Falsifiable prediction: sealed-holdout net expectancy > 0 with bootstrap 95% CI excluding zero and PF > 1.0 at n≥500 exits without raising tail_loss_5pct beyond −$0.60; if either fails, the hypothesis "flatten logic destroys a real gross edge" is rejected and recorded. No code, config, YAML, or champion changes — any surviving variant goes through every `promote.py` gate before it could ever be considered for promotion.