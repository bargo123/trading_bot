Candidate: test a research-only two-consecutive-closed-bar confirmation after the validated-state gate before allowing an entry. In a cost-adjusted walk-forward comparison, accept it only if sealed-holdout expectancy per trade improves by at least 10%, trade count remains at least 80% of baseline, and maximum drawdown does not worsen by more than 5%. Record the ablation in `ExperimentRegistry`; do not modify the champion.