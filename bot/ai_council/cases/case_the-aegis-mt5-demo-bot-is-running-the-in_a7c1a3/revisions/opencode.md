**Proposal:** Run one pre-registered demo-only exit experiment: on a frozen symbol set (EURUSD, XAUUSD) and regime band (ATR-percentile 20–80) over a fixed sampling window, replace quick-win/never-green flattens with a trailing stop that arms at +0.8R breakeven and trails 1.0×ATR(14) behind the extreme close, SL unchanged — all parameters, window bounds, cost model (runner-recorded spread/slippage per fill), and adaptation rules (any post-hoc change voids the run and is logged as a separate rejected variant) committed to a spec in `bot/research/strategies/` before any holdout is touched. The single primary endpoint, declared ex ante with no multiplicity: net-of-cost expectancy > 0 on ≥300 sealed holdout round-trips, tested at two-sided α=0.05 via a one-day stationary block bootstrap (p05 > 0), jointly requiring payoff ratio ≥ 0.6 and win rate ≥ 55%, with a pre-unsealing power check confirming n≈300 gives ~80% power for Δexpectancy ≈ 0.05 given realized exit variance. Holdout integrity is enforced structurally: chronological split with a ≥30-day embargo gap and per-symbol stratification so temporally/instrumentally correlated trades cannot leak between dev and holdout. "Demo-only" is technically enforced rather than asserted — the experiment runner whitelists writes to `bot/research/strategies/`, `bot/research/sealed_holdouts.jsonl`, and `bot/reports/research/`, asserts against the `PROTECTED_LIVE_YAML` list, uses MT5 strictly read-only, and leaves champion promotion impossible unless every gate in `bot/aegis/research/promote.py` passes plus explicit human approval. Failure flips nothing: the challenger and its full metrics are recorded as rejected via `ExperimentRegistry` and the sealed-holdout log, keeping the falsification discipline of Aronson's *Evidence-Based Technical Analysis* while testing the *Technical Analysis of Stock Trends* "let profits run" prescription.