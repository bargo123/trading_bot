Test the two-consecutive-bar persistence requirement in a registry-recorded, read-only shadow experiment, pre-registering before unsealing the sealed holdout the minimum sample size, confidence level, significance test, 10% expectancy threshold, 20% signal-count limit, and a strict holdout-reuse budget. Re-timestamp entries to the persistence-confirmed bar and recompute spread/slippage and all outcomes so the comparison reflects the actual traded population and exposure. Accept the challenger only if the pre-specified statistical test supports at least a 10% improvement in net expectancy per signal, while aggregate net PnL is non-inferior or improved, signal count declines no more than 20%, and drawdown and tail-risk metrics remain within pre-registered limits. Promotion additionally requires every standard `promote.py` gate—including walk-forward stability, regime-slice robustness, and cost-adjusted expected value—to pass; otherwise reject the challenger and leave the champion unchanged.