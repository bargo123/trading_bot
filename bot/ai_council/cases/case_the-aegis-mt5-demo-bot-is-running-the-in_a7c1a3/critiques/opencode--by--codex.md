The “pre-registration” is incomplete: breakeven timing, trailing distance, symbols, regimes, sampling window, cost model, and adaptation rules are unspecified, allowing post-hoc tuning and overfitting to the observed exit labels. A ≥300-trade holdout may still be temporally or instrumentally correlated, and “bootstrap p05 > 0,” “win rate not collapsing below ~55%,” and the expectancy/payoff gates are ambiguous without a predefined test, confidence level, multiplicity control, and power analysis. “Demo-only” and “no live config edits” are asserted rather than technically enforced, so the proposal needs an explicit path-level safety check preventing the challenger from reaching live YAML, order placement, or champion promotion.