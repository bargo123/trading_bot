Proposal: Run an exit-side research cycle targeting the payoff-geometry deficit in `outcome_learning.md` (win rate 67.5% but expectancy −0.012 and PF 0.77, avg_loss ≈ 3× avg_win), using `bot/aegis/research/exit_research.py` over the reconciled outcome log joined with read-only M1 bar history. Before any simulation, verify the data actually supports intratrade replay (per-bar high/low around each position's entry/SL); if only terminal outcomes exist, record the data gap in `ExperimentRegistry` and stop rather than fabricate counterfactual paths. Replay semantics will be fixed and documented up front: triggers fill at the next bar open, every exit pays runner spread/slippage, gaps close at the first post-gap open with no favorable intra-gap SL assumption, simultaneous SL/time-stop conflicts resolve to the worse price, and "manual" exits are defined as those lacking an automated-rule match and are excluded from rule-attribution metrics. The N/M/K grid is chosen solely on train-period data (≤ 2026-06-15) via walk-forward folds spanning distinct regimes; the post-cutoff holdout is then evaluated exactly once on the single pre-registered config, with results committed before any further slicing. Promotion requires expectancy > 0, PF > 1, and bootstrap 5th-percentile expectancy > 0 net of costs on both the worst walk-forward fold and the untouched holdout (win rate reported diagnostically only), followed by every gate in `promote.py`; failure is recorded and sealed, with no code changes, no champion edits, and no live YAML touched.