Proposal: Run an exit-side research cycle targeting the measured payoff-geometry deficit in `outcome_learning.md` (win rate 67.5% yet expectancy −0.012 and PF 0.77 because avg_loss ≈ 3× avg_win, tail_loss 1.21 erases ~20 wins, SL closes run at exp −0.55, and 76% of exits are "manual" rather than automated). Concretely: use `bot/aegis/research/exit_research.py` to grid-test a hard time-stop plus breakeven-move rule (close any position not ≥ +N pips within M bars; move SL to entry at +K pips) over the existing 3,370-row reconciled outcome log with a time-split train/holdout like the ML filter used (train ≤ 2026-06-15, holdout after). Falsifiable prediction: on the untouched holdout, expectancy flips from −0.012 to > 0 with PF > 1 while win rate stays ≥ 60%, net of runner spread/slippage, and bootstrap 5th-percentile expectancy > 0 (the same criterion that rejected the ML filter candidate). If the holdout passes every gate in `promote.py` it becomes a governed challenger; if it fails, record the rejection in `ExperimentRegistry` and seal it per protocol. No code changes, no champion edit, no live YAML touched — this is a replay-based shadow experiment only, consistent with the expectancy-first framing in *Trade Your Way to Financial Freedom* (no direct corpus passages matched this case).