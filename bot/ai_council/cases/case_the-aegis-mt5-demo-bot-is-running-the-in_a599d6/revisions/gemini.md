To resolve the "magic number" critique, I propose a sensitivity grid search (1.5x to 5.0x spread) to empirically determine the reward-to-cost threshold that maximizes expectancy for the MT5 DEMO firehose. This investigation will utilize a walk-forward protocol with a 40% sealed-holdout window to mitigate selection bias and prevent multiple-comparison leakage into `validated_states.json`. Success is redefined as identifying at least three configuration clusters that maintain a cost-adjusted bootstrap p05 > 0 on OOS data while accounting for spread, simulated slippage, and exit symmetry. The experiment will be formally logged in the `ExperimentRegistry` and remains research-only, requiring a full pass through all `promote.py` gates before any champion artifacts are updated. This rigorous approach treats the current "zero validated states" as a valid null hypothesis, ensuring that any surviving configuration represents durable alpha rather than a lucky survivor of random-walk fluctuations.