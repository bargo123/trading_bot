Proposal (revised): Test a hard tail-loss cap as a challenger exit for CORE_STRATEGY_V1 by selecting k ∈ {5, 10, 15, 20} and an ATR-scaled variant strictly on IS data via `exit_research`, validating only the single winning k once on OOS, and touching the sealed holdout exactly once at the end under a multiple-testing correction across the candidate set. Because truncating losses changes the exit distribution, recompute the post-truncation hit rate and break-even loss from replayed IS trades rather than assuming the current 95.7% persists, and gate on net-of-costs expectancy, PF, and drawdown/tail metrics (max DD, CVaR) instead of any win-rate floor. To make costs non-theoretical, replay all stop fills under a stressed friction model (e.g., 2–3× runner spread/slippage, reflecting tail-event expansion) and require the challenger to stay expectancy-positive under that stress. Report bootstrap CIs on expectancy/PF, enforce minimum per-cell trade counts, and include per-regime sensitivity, treating the single observed tail loss as motivation rather than statistical evidence. Falsification: if no candidate clears all of these gates, reject and log via ExperimentRegistry, keeping the challenger on record and never promoting unless every `promote.py` gate passes. Research-only; no live trading, config, or champion changes.