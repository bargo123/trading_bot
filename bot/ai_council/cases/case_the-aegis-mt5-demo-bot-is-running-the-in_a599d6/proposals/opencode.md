Proposal: Test a hard tail-loss cap as a challenger exit for CORE_STRATEGY_V1 in the next cycle: grid-search a maximum-loss stop at k × the current TP distance (k ∈ {5, 10, 15, 20}) plus an ATR-scaled variant, replayed through `exit_research` on the same IS/OOS split and then the sealed holdout, net of runner spread/slippage. The measured case for it is unambiguous — the champion's 95.7% win rate is cosmetic because avg_win is 0.031R while avg_loss is −2.57R (PF 0.27, expectancy −0.027R), and the reconciled demo exits show one tail loss erases ~20 average wins (`outcome_learning`, cosmetic_win_rate=False); break-even requires cutting avg_loss to ≤ ~0.7R at the current hit rate. Falsification criterion: if no k in the grid lifts holdout net-of-costs expectancy above 0 R and PF above 1.0 without dropping win rate below 85%, the tail-stop hypothesis is rejected and recorded via ExperimentRegistry, with the challenger kept on record and never promoted unless all `promote.py` gates pass. This is consistent with the corpus's expectancy/R-multiple framing ("Trade Your Way to Financial Freedom") and its tail-risk emphasis ("Stock Market Wizards"). No live trading, config, or champion changes involved — research-only.