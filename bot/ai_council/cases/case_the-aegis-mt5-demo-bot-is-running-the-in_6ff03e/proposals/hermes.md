Candidate improvement: recalibrate the validated-state gate from assumed costs to measured costs. For every DEMO firehose order already logged, pull actual entry/exit spread and slippage from the broker trade records and compare them against the runner's cost assumptions currently used by the promote.py economics; then rerun the split-primary OOS and sealed-holdout evaluations with measured costs substituted. Falsifiable criterion, pre-registered: if champion ranking, gate pass/fail status, and net expectancy all shift by less than a fixed tolerance (e.g., ±0.1R per trade), the cost model is validated and this line is closed; if the sitting champion fails its net-EV gate under measured costs, or a previously rejected challenger would have passed, the next cycle adopts cost-recalibrated gating as the primary change. This requires only reading existing logs through the research path — no code changes to execution, no config edits, no new orders — and directly stress-tests the requirement that outcomes be net of the runner's spread/slippage assumptions. The recently exposed split primary OOS economics, multi-outcome metrics, and per-candidate executable-overlap horizons make this comparison measurable now rather than later.