DEMO fills may be sparse and regime-specific, so recalibrating from them risks fitting costs to a nonrepresentative execution sample. The ±0.1R threshold is arbitrary and lacks confidence intervals, minimum sample sizes, and a definition for reconciling broker records with modeled fills. Reusing sealed holdouts to choose a new primary gate would contaminate them; the proposal needs a fresh untouched validation set before changing promotion criteria.