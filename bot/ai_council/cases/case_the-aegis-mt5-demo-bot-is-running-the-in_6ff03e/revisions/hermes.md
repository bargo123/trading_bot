Pre-registered freeze before any data access: commit to the experiment registry, in advance, the full candidate roster (sitting champion plus every previously rejected challenger with its original frozen spec), the cost-reconciliation rules (one-to-one matching of broker deal records to logged orders; partial fills aggregated volume-weighted; commissions included; spread from the runner snapshot nearest fill time; unmatched or missing records excluded from statistics and reported as an explicit coverage ratio), and all decision thresholds. Proceed only if realized fills meet a pre-registered minimum — e.g., ≥200 closed trades spanning ≥3 volatility regimes and both session blocks — otherwise register "cost model unvalidated" and leave the assumed-cost gate unchanged, since sparse or regime-specific samples trigger no recalibration. Replace point substitution with uncertainty-aware evaluation: rerun split-primary OOS under bootstrap-resampled measured-cost distributions, report net-expectancy deltas with 95% CIs, and declare the cost model validated only if every ranking, gate-status, and net-EV shift falls inside a tolerance that must itself exceed the CI half-width (superseding the bare ±0.1R). Protect holdout integrity: sealed holdouts are never used to choose the change — adoption of cost-recalibrated gating is decided solely on split-primary OOS plus a fresh untouched validation window drawn from data after the calibration cutoff, with any sealed holdout opened only after adoption as one-shot confirmation under existing promote.py governance. If adequate-sample measured costs flip any outcome beyond tolerance, the next cycle adopts cost-recalibrated gating as its primary change; all results, including nulls and coverage shortfalls, are recorded honestly in ExperimentRegistry.