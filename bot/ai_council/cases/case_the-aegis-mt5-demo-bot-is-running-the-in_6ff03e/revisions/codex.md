Test a pre-registered spread-adjusted EV gate whose threshold is selected from training data or prior research, with 1.5× evaluated only as one fixed candidate in a sensitivity analysis—not tuned on sealed holdouts. On a fixed forward holdout with a declared duration and minimum trade count per instrument/regime cell, compare against the current gate using net expectancy per trade and maximum peak-to-trough drawdown, requiring pre-specified minimum effect sizes, non-overlapping 95% confidence intervals or an adjusted hypothesis test, and validated-state precision no worse than 5% relative. Define regimes before evaluation, apply multiplicity correction across instruments, regimes, and threshold candidates, and reject the gate if power or sample-size floors are unmet or results are not robust across the pre-declared cells. Compute costs from the runner’s actual spread/slippage model calibrated to realized fills, include a pre-specified spread-spike stress test, and record the complete protocol, data, and outcome in ExperimentRegistry before unsealing results.