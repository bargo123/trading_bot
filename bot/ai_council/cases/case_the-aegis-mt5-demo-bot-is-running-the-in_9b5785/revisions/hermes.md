Candidate improvement: a spread-percentile abstention gate, tested shadow-first before any code change. Hypothesis: entries opened when live spread at signal time falls in the top quartile of a pre-registered trailing distribution — per-symbol, trailing 30 days of signal-time spreads with a minimum of 100 observations, computed strictly point-in-time from data available at entry — realize materially lower mean net-of-cost EV than lower-quartile entries, enough that abstaining raises aggregate net expectancy even after forfeiting the skipped trades' positive-EV volume. Method: retrospectively bucket confirmed trades in outcome_log.jsonl using exactly that frozen definition, excluding all sealed-holdout periods from estimation, with a single pre-registered primary contrast (top vs. bottom quartile, two-sided bootstrap 95% CI, minimum 200 confirmed trades per bucket), time-ordered train/validation splits, Holm correction on any secondary slices, and confound diagnostics stratifying by session hour, symbol, scheduled-news windows, and signal type to demonstrate a residual spread effect rather than a volatility/session proxy. Decision rule (falsifiable): advance to shadow validation only if the top-quartile net-expectancy CI lies wholly below zero, the bottom-quartile CI lies above zero, the sign pattern replicates out-of-sample and across symbols/regimes without flips, and simulated abstention beats the realized trade book on aggregate net EV after charging skipped winners — otherwise record the null honestly in ExperimentRegistry and drop the candidate for the cycle. Any resulting shadow gate runs as a read-only research process fully isolated from the sole MT5 execution owner, writing nothing but its own study artifacts under bot/reports/research/ — never runner, config, or champion state. Champion adoption still requires the gated policy to beat the ungated intelligent champion on sealed holdout data through every promote.py gate before any champion update.