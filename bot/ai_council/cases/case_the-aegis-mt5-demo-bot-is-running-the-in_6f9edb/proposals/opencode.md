Proposal: The measured evidence points at exit geometry as the dominant drag — of the 2,517 reconciled demo exits, 76% are `manual` closes (PF 0.64), average winners are cut at +0.06 while the worst tail loss runs −1.21 (wins_erased_by_tail_loss ≈ 20), and overall expectancy is −0.012 despite a 67.5% win rate. For the next cycle, run an offline counterfactual in `exit_research` that replays the identical entries from `intel/outcome_log.jsonl` under one rules-based exit policy (e.g., symmetric 1R stop / 2R target or an ATR-trailing stop, net of runner costs) instead of manual closes. Acceptance criterion: bootstrap expectancy improves from −0.012 to ≥ +0.02 with PF ≥ 1.1 held out-of-sample per symbol (USDCHF, USDCAD, GBPUSD first); if any variant fails to beat the manual-close baseline after costs, record the rejection in the ExperimentRegistry and keep the current behavior. This is consistent with the corpus's exit-symmetry and tail-loss themes (e.g., *Trade Your Way to Financial Freedom* on R-multiple exits; *Stock Market Wizards* tagged `tail_loss`) and requires no champion change and no gate loosening — notably, `validated_states.json` currently shows n_survive=0, so fixing attribution first will also give the validated-state gate cleaner inputs.