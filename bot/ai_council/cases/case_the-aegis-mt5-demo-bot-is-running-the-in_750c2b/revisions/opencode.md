Test a stricter validated-state entry gate requiring positive net expected value after modeled spread/slippage and confirmation from two consecutive independent scans before a DEMO entry signal is eligible. Evaluate it against the current gate on sealed holdout data with identical costs and no holdout tuning. Use a supported model configuration for the evaluation pipeline, and reject the change unless net expectancy improves without worsening maximum drawdown or false-positive entry rate.