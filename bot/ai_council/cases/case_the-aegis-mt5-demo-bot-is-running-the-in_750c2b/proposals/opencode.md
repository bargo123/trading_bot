Test a stricter validated-state entry gate requiring both a positive net expected value after modeled spread/slippage and confirmation from two consecutive independent scans before a DEMO entry signal is eligible. Compare it with the current gate on sealed holdout data, using identical costs and no parameter tuning on the holdout. Reject it unless net expectancy improves while maximum drawdown and false-positive entry rate do not worsen.