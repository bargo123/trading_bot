Revised proposal: Run the dormant `exit_research` module over the 2,518 reconciled demo exits to test one challenger — a hard loss cap whose value is frozen from training folds only (selected via a pre-registered rule on in-fold outcomes, e.g., ~2× in-fold average win), applied out-of-sample through the existing cost-aware nested walk-forward, with the sealed holdout evaluated exactly once after thresholds are locked. Acceptance is pre-registered and multi-dimensional: net expectancy ≥ 0 with a bootstrap 95% CI excluding material losses, win-rate decline ≤ 2 pp, minimum per-fold sample sizes, stable sign across regime splits, and flat/sensible behavior across a cap-value sensitivity grid (1.5×–3× average win) so no single value drives the result. Because reconciled final exits cannot prove an intratrade stop would have filled, the study is staged: first confirm the dataset contains intratrade paths (MFE/MAE or bar-level series); if absent, results are labeled counterfactual, registered as exploratory-only, and ineligible for promotion. Where paths exist, stop fills are modeled worst-case within the touching bar and charged full measured spread/slippage, so claimed capped outcomes are conservative and genuinely net of costs. Only a challenger passing every gate in `promote.py` on these verified fills advances; anything else stays on record with the champion untouched — still targeting the dominant measured failure (payoff_ratio 0.34), but with interval-, robustness-, and executability-gated claims rather than point estimates.