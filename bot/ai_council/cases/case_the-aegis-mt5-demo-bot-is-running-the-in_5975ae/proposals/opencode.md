Proposal: Run the currently dormant `exit_research` module (it shows `n_rows: 0` in ml_pipeline.json) over the 2,518 reconciled demo exits to test one challenger: a volatility-scaled hard loss cap set at ~2× the observed average win (~0.12 R, vs today's avg_loss of −0.177 and tail_loss of 1.21 that erases ~20 winners). Falsifiable prediction: capping the worst-decile losers flips aggregate net expectancy from −0.012/PF 0.77 to ≥ 0 on the sealed holdout, net of measured p75 costs, without materially cutting the 67.5% win rate from TP-style exits (+0.107 expectancy) that already work. Evaluate strictly via the existing cost-aware walk-forward and all gates in `promote.py`, registered in `ExperimentRegistry`; a rejected challenger stays on record and the champion remains untouched. This targets the single dominant measured failure — payoff geometry (payoff_ratio 0.34), not entry selection — where only 2 of 43 shortlisted strategies survive validation anyway.