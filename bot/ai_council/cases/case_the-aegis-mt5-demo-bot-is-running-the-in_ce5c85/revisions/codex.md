Pre-register one fixed two-consecutive-snapshot gate, with snapshot cadence, validated-state criteria, invalidation, and scratch classification defined independently using the existing exit logic; derive the minimum sample size, meaningful reduction, and non-inferiority expectancy margin from the fixed baseline window, variance, cost model, and risk budget before evaluation. Evaluate all baseline signals using intention-to-treat counterfactual outcomes, net of spread, slippage, missed fills, and added latency, with bootstrap confidence intervals, pre-specified significance testing, regime stratification, and a sealed out-of-sample holdout over a fixed window. Record the study in ExperimentRegistry, and apply Holm/Bonferroni correction if any alternative snapshot counts or definitions are examined rather than tuning until success. In DEMO shadow mode, retain an unchanged control and record timestamps, quotes, decisions, and counterfactual fills; do not alter the champion or protected execution YAML unless the designated operator explicitly authorizes it. Reject the gate unless the pre-registered primary endpoint meets its confidence-bound and economic criteria in replay, shadow mode, and the sealed holdout.