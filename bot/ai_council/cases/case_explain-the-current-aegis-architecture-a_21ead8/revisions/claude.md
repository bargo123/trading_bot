Aegis is a three-layer, file-mediated system: a runtime layer (`scripts/run_broker_paper.py` → `aegis/mgc_firehose.py` with `aegis/intel/` brain modules, driven by `config_mt5_demo_firehose_hw.yaml`), a reconciliation layer (`aegis/reconcile.py`, mapping broker deals to `intel/outcome_log.jsonl` and `reports/*_journal.jsonl`), and a research/governance layer (`aegis/research/*`, `scripts/research_*`, `aegis/optimizer/`) whose outputs gate champion promotion, with the fast watcher and council as read-only advisors. The signal worth chasing is still the exit path — `outcome_learning.md` shows 1891/2404 exits (79%) land in the `manual` bucket at expectancy −0.0165 / PF 0.567 against +0.090 for broker-hit `tp` — but I was wrong on two counts the critique names: `reports/firehose_mfe.json` is a per-symbol live `dict[str, float]` whose keys are popped on close (`exits.py:205-224`), so there is no per-ticket excursion history to join, and `close_reason()` (`reconcile.py:234-235`) collapses every `aegis_flatten` comment into `"manual"`, bundling giveback, quick_win, never_green, max_hold, and session flatten into one cohort whose members have opposite implications. So the revised ask is explicitly a build, not an observation, in two costed stages: (1) a small instrumentation change that captures peak favorable and adverse excursion per ticket at flatten time plus the specific flatten sub-rule, appended to a durable per-exit log, and (2) a separate reconcile change that propagates the sub-rule into `close_reason()` so the bucket splits at least five ways in `outcome_learning.json`; only stage 1's write path touches runtime code, and it emits data without reading back into any decision. Historical backfill of the 1891 prior exits cannot be reconstructed from current artifacts — `firehose_mae.json` is `{}` and MFE was never persisted — so the honest options are approximate reconstruction from MT5 M1 bars between journal entry/exit timestamps (coarse, and it will miss intrabar peaks) or accepting that the test only runs forward on newly instrumented exits; I recommend the forward path and would expect a usable read in roughly two weeks at current fill volume. The falsifiable prediction now attaches per sub-rule rather than to the blended cohort: at least one flatten sub-rule (my prior is max_hold or giveback) will show ≥40% of its exits reaching ≥1.0× the configured TP distance in MFE with median give-back above 0.5R, while quick_win will not — if no sub-rule clears that bar, the flatten family is exonerated and the defect lies in entry timing or TP placement. Nothing here promotes a champion, alters config, or sends live orders; the council decision I need is approval of the instrumentation build and its cost, not of a measurement.