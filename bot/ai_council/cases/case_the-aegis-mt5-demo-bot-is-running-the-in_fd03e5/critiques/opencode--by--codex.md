The proposal risks overfitting by targeting `quick_win`/`never_green` and “validated states” selected from the same 2,518 exits, with no time-based split, purge/embargo, regime coverage, or multiple-testing correction. Its “most closes” claim is internally unclear because 1,383 + 1,288 exceeds the sample size, and it omits uncertainty around payoff estimates, execution costs, anchor selection, and whether the sealed holdout was untouched during design. It also does not demonstrate isolation from immutable live YAML/runtime paths, so “no promotion” alone is insufficient evidence that the challenger cannot affect live trading.