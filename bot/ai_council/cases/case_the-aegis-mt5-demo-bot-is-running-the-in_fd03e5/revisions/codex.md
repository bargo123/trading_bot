Test one pre-registered, research-only challenger with all thresholds frozen before evaluation: reject validated-state signals when a pre-existing, out-of-sample-calibrated edge model predicts edge below 1.5× the runner’s modeled round-trip spread/slippage cost. Record the hypothesis, derivation, calibration status, splits, and run in `ExperimentRegistry`; evaluate on identical walk-forward splits and one sealed-holdout pass, with costs swept from 1.25× to 3×. Require at least the minimum pre-specified trade count in every evaluation bucket and bootstrap confidence intervals excluding zero for net-expectancy improvement in every fold, while meeting the aggregate ≥10% expectancy gain and no more than 0.05 profit-factor decline. Account for all registered challenger tests in the multiple-testing procedure, and permit promotion only through the governed `promote.py` gates; otherwise retain the current champion unchanged.