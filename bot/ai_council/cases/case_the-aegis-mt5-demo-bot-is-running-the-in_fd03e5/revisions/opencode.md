**Revised proposal:** Build an exit-geometry challenger in `exit_research` for the previously validated states, but fit and tune it only on an earlier time-based development slice of the 2,518 exits, separated from the sealed holdout by a purge/embargo gap — the holdout stays untouched during design, verifiable via ExperimentRegistry timestamps — and report all expectancy/payoff estimates with bootstrap CIs given the modest per-bucket sample sizes. Correcting my earlier claim: `quick_win` (1,383) and `never_green` (1,288) are overlapping multi-label audit tags, not disjoint close counts, so they are restated as tag frequencies rather than shares of exits. To guard against overfitting, the candidate is evaluated across *all* state/regime buckets (not only those selected as validated) and the anchor/time-stop parameter grid carries a Bonferroni-style correction across the tests actually run. Pass criteria remain net of runner spread/slippage and are stress-tested at 1× and 2× assumed costs: holdout `expectancy_validate > +0.00` (vs −0.012 baseline), `bootstrap_p05 > 0`, and PF > 1.0. Isolation is structural, not just procedural — the challenger code and artifacts live exclusively under `bot/aegis/research/` and `bot/research/`, read/write no live YAML or runtime intel paths, and reach `intel/intelligent_champion.json` only through the governed `promote.py` gates; failures are recorded honestly in the registry with no promotion.