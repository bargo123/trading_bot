Proposal: Run an exit-geometry challenger against the measured payoff asymmetry — across the 2,518 reconciled demo exits (outcome_learning.json) win rate is 67.5% yet expectancy is −0.0122 and PF 0.77 because payoff ratio is only 0.34 (avg win 0.060 vs avg loss −0.177); by close reason, `tp` exits earn +0.107 each while `manual` flattens (n=1928, PF 0.64) and `sl` hits (−0.547 each) destroy the edge, and the failure audit attributes most closes to `quick_win` (1383) and `never_green` (1288). Concretely: build an exit_research candidate for the two validated states (asia/range/sell among others) that suppresses the quick-win early flatten and holds to the existing TP anchor unless an ATR-based invalidation or a fixed time-stop fires, registered in ExperimentRegistry and evaluated net of runner spread/slippage on the sealed holdout. Falsifiable pass criterion: holdout expectancy_validate > +0.00 (vs current −0.012 baseline), bootstrap_p05 > 0, and PF > 1.0; any failure is recorded honestly and no promotion occurs through the governed promote gates. This follows Van Tharp's *Trade Your Way to Financial Freedom* (2006) from the corpus, which scores 20 risk-topic hits and frames expectancy as win-rate × average win minus loss-rate × average loss — exactly the term our exits are currently flipping negative.