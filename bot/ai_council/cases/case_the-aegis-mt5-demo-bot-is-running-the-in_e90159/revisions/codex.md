Pre-register the validated-state definition, session boundaries, spread/liquidity cutoffs, and threshold levels before running a costs-inclusive walk-forward DEMO/research replay, then evaluate once on a sealed out-of-sample holdout with minimum sample floors for each stratum. Compare session-aware gating against both the current gate and a flat globally raised threshold calibrated ex ante to the same overall abstention/trade-count target, using executable spread/slippage assumptions and unchanged sizing. Define per-trade and portfolio adverse excursion precisely, and require bootstrap confidence intervals showing a practically meaningful reduction in excursion and improvement in net expectancy, with trade-count-adjusted expectancy non-inferior and abstention below the pre-registered limit. Check concentration and correlation by session regime, and promote only if all criteria pass and the sanctioned promotion scripts confirm every `promote.py` gate.