The proposal's session boundaries and threshold levels are free hyperparameters: without pre-registering them before the walk-forward and validating on a sealed holdout, the result is tunable-until-it-passes, and rare low-liquidity strata likely yield tiny samples where "improved expectancy" is noise. Critically, the control arm is wrong — raising any EV threshold mechanically lifts cost-adjusted expectancy by culling wide-spread trades, so the candidate must beat a flat globally-raised threshold, not just the current gate, or the session-awareness claim is untested. Key terms are unmeasurable as written: "adverse excursions" (which metric, per-trade or portfolio?), "improves net expectancy" (no effect size, significance test, or bootstrap), and the abstention "predefined limit" with no evidence it was fixed before results were seen. No safety violation — DEMO/research replay and registration comply with policy — but promotion must still clear every promote.py gate through the sanctioned scripts regardless of a favorable replay.