Counterfactual rejection audit of the validated-state gate: during the next research cycle, log every firehose candidate state the gate rejects (stale quote, unvalidated feature state, etc.), then offline-replay each rejected state through the current champion's entry/exit logic to compute its counterfactual net EV after realistic spread/slippage, benchmarked against the realized net EV of admitted trades over the same sessions. Falsifiable hypothesis: the rejected population's mean counterfactual net EV per trade is not significantly above zero (bootstrap 95% CI spanning zero over ≥200 logged rejections), implying the gate's strictness costs little edge and its threshold is correctly set; the alternative outcome — rejected-state EV significantly exceeding admitted-trade EV — would demonstrate the gate is filtering out real alpha, in which case a single-step relaxation becomes the next challenger, run through the full promote.py gate suite against the sealed holdouts before any consideration. Both arms are recorded in ExperimentRegistry, all computation stays read-only against existing outcome/broker-paper logs, and the champion remains untouched either way.