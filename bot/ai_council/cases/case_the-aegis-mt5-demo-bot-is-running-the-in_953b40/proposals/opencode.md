Test whether requiring a second independent validated-state confirmation within 60 seconds reduces false entries versus the current single-confirmation gate on sealed MT5 DEMO replay data. Accept it only if net expectancy after configured spread/slippage improves and trade count remains at least 70% of baseline; otherwise reject it. Record the experiment and keep the current champion unchanged.