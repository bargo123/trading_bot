Test a second independent validated-state confirmation within 60 seconds against the single-confirmation gate using sealed MT5 DEMO replay data. Use a supported, configured research model or deterministic local evaluation path; if model access fails, record the infrastructure failure without producing metrics. Accept only if net expectancy after configured spread/slippage improves and trade count remains at least 70% of baseline; otherwise reject it. Record the experiment and keep the current champion unchanged.