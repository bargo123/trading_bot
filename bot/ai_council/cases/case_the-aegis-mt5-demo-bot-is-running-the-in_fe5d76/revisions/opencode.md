Test a volatility-regime filter that abstains from entries when 15-minute ATR exceeds the 90th percentile of its trailing 60-day distribution. Evaluate it on sealed historical holdouts against the current validated-state baseline, net of configured spread and slippage. Record the experiment and any infrastructure failure honestly; use only supported, authenticated research-model endpoints if model assistance is required. Advance it only if maximum drawdown falls at least 15% without reducing net expectancy or trade count by more than 10%.