Test the shadow-only two-source confirmation gate using a frozen, pre-registered specification in which the analogue/outcome model has disjoint training data, labels, features, and tuning windows from the validated-state gate, with all holdouts sealed before evaluation. Evaluate against the current gate across multiple temporal and market-regime holdouts, requiring at least 200 independent eligible signals per arm and regime. Use paired block-bootstrap confidence intervals at α=.05, with Holm correction across the primary expectancy, coverage, drawdown, and calibration comparisons. Accept only if the expectancy ratio’s lower 95% bound exceeds 1.10, coverage’s lower bound is at least 0.80 of baseline, drawdown does not worsen within the confidence bound, and calibration is non-inferior under the pre-specified test; otherwise reject it and leave the champion unchanged.