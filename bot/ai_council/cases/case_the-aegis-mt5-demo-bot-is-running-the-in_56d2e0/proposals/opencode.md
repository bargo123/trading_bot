Test a session-specific spread filter: abstain from entries when the current spread exceeds the rolling 20-trade median spread by 25%, while preserving all existing validated-state gates. Evaluate it on sealed historical windows with the runner's configured spread and slippage costs, comparing net expectancy, maximum drawdown, trade count, and invalidation rate against the current champion. Accept it only if net expectancy improves and maximum drawdown does not worsen, with no degradation in the holdout result.