Test a research-only challenger in which “validated-state” means the existing validation predicate evaluated on completed bars using only information available at each close; the first true close is confirmation one, the next consecutive true close is confirmation two, and the signal becomes eligible only at the following bar’s open, with any false state resetting the count. Evaluate it once on the sealed chronological holdout using unchanged spread/slippage and fill assumptions, preregistering a minimum uplift of 0.05R per trade, a 95% bootstrap confidence interval whose lower bound exceeds zero, at least 200 trades overall and 30 per predefined market regime, and a regime-by-regime report. Measure drawdown as peak-to-trough drawdown of the net equity curve, requiring it to be no more than 0.5 percentage points above the incumbent, while also reporting delayed or missed trades and fill changes caused by the extra bar. Register the experiment before evaluation; if every gate fails, reject the challenger and leave the champion unchanged.