Test a stricter validated-state entry filter that excludes trades when the current spread exceeds the trailing 75th-percentile spread for that symbol/session. Evaluate it on the next sealed holdout with identical signals, costs, and exits. Accept it only if net expectancy improves by at least 10% while trade count remains at least 70% of baseline and max drawdown does not increase.