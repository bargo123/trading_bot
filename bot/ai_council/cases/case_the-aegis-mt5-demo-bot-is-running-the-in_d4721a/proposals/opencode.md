Test a stricter validated-state confidence threshold that abstains from entries when recent analogue coverage is sparse. Accept it only if the sealed holdout improves net expectancy after modeled spread/slippage without increasing maximum drawdown or reducing trade count by more than 30% versus the current champion. Record the experiment and retain the incumbent if any gate fails.