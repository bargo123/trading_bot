Test whether a regime-conditioned spread threshold improves net expectancy: reject entries when the current spread exceeds the rolling 80th percentile for that symbol and session. Backtest this gate on sealed holdouts with existing slippage assumptions, comparing net expectancy, tail loss, trade count, and drawdown against the current validated-state baseline. Adopt it only if net expectancy improves and neither tail loss nor drawdown worsens.