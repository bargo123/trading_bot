Test whether a regime-conditioned spread threshold improves net expectancy, using supported research infrastructure and authenticated model endpoints only. Reject entries when the current spread exceeds the rolling 80th percentile for that symbol and session, then evaluate the gate on sealed holdouts under existing slippage assumptions. Compare net expectancy, tail loss, trade count, and drawdown with the current validated-state baseline. Adopt only if net expectancy improves without worsening tail loss or drawdown.