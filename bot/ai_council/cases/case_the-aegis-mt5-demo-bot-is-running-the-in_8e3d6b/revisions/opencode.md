Test the validated-state persistence filter using a supported, configured model rather than `x-preview-f-free`. Require two consecutive qualifying scans before a signal becomes trade-eligible in MT5 DEMO. Compare it with the current gate on a sealed, cost-adjusted holdout, measuring net expectancy, maximum drawdown, and trade count. Adopt it only if net expectancy improves, drawdown does not worsen, and it retains at least 70% of baseline trades.