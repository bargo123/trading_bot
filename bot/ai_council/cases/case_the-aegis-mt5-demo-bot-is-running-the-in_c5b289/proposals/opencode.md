Test whether requiring two consecutive validated-state observations before entry reduces false starts versus the current single-observation gate. Evaluate on sealed holdouts with identical spread/slippage assumptions; accept only if net expectancy improves and trade count remains sufficient under the existing promotion gates. Keep the change research-only and reject it if either result fails.