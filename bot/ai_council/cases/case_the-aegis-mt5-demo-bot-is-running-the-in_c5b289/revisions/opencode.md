Test whether requiring two consecutive validated-state observations before entry reduces false starts versus the current single-observation gate. Run the evaluation on sealed holdouts with identical spread/slippage assumptions using a supported model or existing local research tooling. Accept only if net expectancy improves and trade count remains sufficient under existing promotion gates; otherwise reject the change and keep it research-only.