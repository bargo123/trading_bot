The proposal is selection-biased and overfit: it targets buckets and exit rules chosen after inspecting the same 2,517 outcomes, then tries unspecified values of N and loss caps, with no nested validation or multiple-testing correction. The outcome log may not contain intratrade bar paths needed to replay counterfactual time-stops or tightened stops, making the claimed results unmeasurable unless reconstructed from point-in-time market data with spread, slippage, and stop-execution modeling. A single sealed-holdout pass after tuning is insufficient, and the “win rate not materially hurt” claim lacks a predefined threshold, sample-size/power requirement, and bucket-level stability gates. Safety is acceptable only if the work remains offline, every trial—including failures—is registered, protected live YAML is untouched, and promotion occurs exclusively through the governed scripts.