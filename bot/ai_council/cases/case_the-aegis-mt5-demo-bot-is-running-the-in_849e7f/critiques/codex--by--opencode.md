The decile cutoff is an optimization degree of freedom unless it was fixed before inspecting the holdout, so it can still overfit despite the registered ablation. “Improves expectancy and drawdown” lacks predefined statistical confidence, minimum effect sizes, and a drawdown measurement method, making promotion discretionary. The 25% trade-count limit does not validate regime stability or account for adverse selection from filtering entries.