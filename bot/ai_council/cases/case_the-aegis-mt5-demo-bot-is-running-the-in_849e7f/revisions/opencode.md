Test a pre-registered ATR-regime filter grid across instruments and rolling walk-forward periods, selecting parameters only within training windows and retaining an untouched sealed holdout for one final evaluation. Compare matched-horizon net expectancy, drawdown, trade count, and exposure against the validated-state baseline under existing spread and slippage assumptions, with predefined minimum samples and confidence/significance bounds. Accept only robust improvements across periods and instruments without materially worsening maximum drawdown or reducing exposure comparability; otherwise reject. Record every run and failure in `ExperimentRegistry`; do not promote or enable runtime execution unless all existing governance gates pass.