Pre-register the fixed margin grid, training-only selection rule, calibration metric, numeric tolerance, minimum trade count, single-use holdout budget, and all prior grid searches in the experiment registry, then evaluate the incumbent and candidate on a fresh untouched holdout. Compare net expectancy and drawdown after spread/slippage costs using paired block-bootstrap confidence intervals that respect trade overlap, with promotion requiring the minimum sample floor and predefined confidence thresholds. Assess trade-validity calibration separately across session and spread/volatility regime strata, not only in aggregate, and require every stratum to remain within the registered tolerance. If the candidate passes these tests, run a forward MT5 DEMO shadow-validation period under the same fixed rules before promotion; otherwise retain the current champion.