Pre-register in ExperimentRegistry: validated state, false start (entry invalidated within 3 bars or stopped before +0.5R), and a fixed two-observation gate, using timestamped recorded MT5 DEMO-feed quotes net of configured spread, slippage, latency, and skipped-trade opportunity cost. On untouched sealed data, compare paired eligible signals with unchanged sizing and exits; require a statistically significant reduction in false-start rate, net expectancy non-inferior within a pre-specified margin, and no material degradation in adverse execution. Require a minimum pre-specified trade count and power, bootstrap confidence intervals for maximum drawdown, and consistent results across pre-defined regime subperiods. The holdout may be touched once; any variant, threshold, or failed result is recorded and requires a new sealed holdout. If accepted, validate only through read-only live MT5 DEMO shadow monitoring before any governed execution change.