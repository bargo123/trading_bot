The 25% threshold and 30-minute window are untuned magic numbers with no sensitivity sweep or justification given, and evaluating a single hand-picked variant on the sealed holdout is textbook selection bias that burns holdout integrity — every variant tried must be logged via ExperimentRegistry with multiplicity handled. "Net expectancy improves" and "without increasing maximum drawdown" are unmeasurable as written: no minimum improvement margin, confidence intervals, or bootstrap significance testing are specified, and drawdown comparisons on a trade set ~20% smaller are dominated by sampling noise. Missing validation: the rolling median must be shown causal (only ticks available at decision time, no lookahead into the current bar), slippage/spread costs must be recomputed on the post-filter trade sequence rather than assumed "identical" since filtering changes which fills occur, and results need regime-stratified checks because a relative spread gate behaves oppositely during rollover/news spikes versus quiet sessions. Finally, no adoption path is named — if implementation means editing bot/config*.yaml directly, that violates the immutable live-YAML rule, so the proposal must state that any adoption routes through the governed promote.py gates and champion protocol.