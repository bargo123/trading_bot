Candidate improvement: add a live-spread/session-liquidity entry filter to the firehose intake path that suppresses entries whenever the live quote spread exceeds the symbol's 75th-percentile trailing-24-hour spread threshold, where the threshold family (percentile × window) is selected once on a pre-registered training slice of the autopsy stream, with all evaluation confined to sealed holdout slices untouched during tuning, stratified by symbol and session, and tested under a Holm-corrected pre-registered hypothesis set to control multiple testing. To eliminate time/regime/opportunity-set confounding, the two-week time-split A/B is replaced by concurrent randomized assignment: every qualifying entry signal is coin-flipped (filter-on vs filter-off) at intake inside the single governed DEMO execution owner (`run_broker_paper.py`), requiring no change to protected live YAML. Both arms are scored net of actual, not assumed, costs — quoted spread at signal time, fill spread, slippage, and commission captured per trade from broker records — and the "guaranteed-negative expectancy" claim is downgraded to a measurable hypothesis that realized-cost-adjusted expectancy in high-spread fills is significantly below that of normal-spread fills. The trial launches only after a power analysis fixes minimum per-stratum sample sizes (≥80% power, α=0.05 two-sided), with the primary endpoints being the fast-loss-count difference (bootstrap CI required to exclude zero, target ≥30% relative reduction), winner retention (filter arm ≥85% of counterfactual winners, pre-defined), and expectancy non-inferiority at a pre-registered margin of no worse than one standard error below baseline computed on realized costs. Interim looks use an O'Brien–Fleming alpha-spending boundary so optional stopping cannot inflate family-wise error, and the full analysis plan is registered in ExperimentRegistry before any data is drawn. Operationally the filter fails closed: stale (>2s) or missing quotes, unit-mismatched/negative spreads, or any runtime gate failure (DEMO account, `allow_live: false`, `mode: mt5_demo`, `paper_trading_enabled: true`) suppresses the entry and logs the event, and a rejected candidate is archived as a failed challenger per promote-gate rules.