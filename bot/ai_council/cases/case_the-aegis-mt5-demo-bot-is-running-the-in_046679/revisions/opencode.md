Sweep pre-registered spread thresholds and causal rolling windows on development data, logging every variant in `ExperimentRegistry`; reserve sealed holdouts for one final multiplicity-adjusted evaluation. Build the median strictly from quotes available before each decision, then recompute spread, slippage, and fills for each post-filter trade sequence under latency, quote-gap, and illiquidity scenarios. Require a pre-specified minimum net-expectancy improvement with bootstrap confidence intervals, multiplicity-adjusted significance, trade-count retention, and non-inferior drawdown assessed with uncertainty bounds. Report results by session and volatility/news regimes, including spread/slippage sensitivity. Any eligible challenger proceeds only through the governed `promote.py` gates and champion protocol; no live YAML is edited.