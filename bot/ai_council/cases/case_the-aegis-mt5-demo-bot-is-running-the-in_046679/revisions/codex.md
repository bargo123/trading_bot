The next cycle will test `failed_break` as the sole challenger, treating it as data-mined rather than pre-specified; before testing, disclose the total number \(N\) of judged configurations and freeze the candidate, filter, symbol weights, regime/session strata, and runner spread/slippage assumptions. Define net expectancy as equal-symbol-weighted mean net R per trade after those locked costs, and evaluate it once on a provenance-documented, untouched holdout matched to development-period regimes and sessions, covering at least 12 months, all 26 symbols, and 200 eligible trades. Require a one-sided 95% family-wise-error-adjusted lower confidence bound above zero, using Holm/Bonferroni \(\alpha=0.05/N\) and the corresponding lower percentile from a 10,000-replicate day-blocked, symbol-cluster bootstrap, plus profit factor above 1, positive median symbol expectancy, and positive expectancy in at least 18 of 26 symbols. The report must document event counts, costs, weighting, regime coverage, stratum results, and evidence that the holdout never informed filter design. Promotion may occur only through `research_promote_champion.py`, after every `bot/aegis/research/promote.py` gate passes and the ExperimentRegistry records the experiment; otherwise, leave the current champion unchanged.