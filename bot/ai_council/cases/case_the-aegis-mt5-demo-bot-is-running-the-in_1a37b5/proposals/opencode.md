Test a stricter validated-state freshness rule: reject entries when the validation snapshot is older than 60 seconds. Compare it against the current gate on sealed DEMO outcomes, net of spread and slippage, requiring a lower adverse-exit rate without reducing net expectancy. Promote nothing unless the improvement is statistically reproducible across at least two non-overlapping holdouts.