Revised proposal: In the next research cycle, first register the experiment via `ExperimentRegistry` and confirm the baseline pytest suite stays green, then run ml_pipeline to populate `validated_states.json` from an earlier, pre-registered survivor cohort—not the same strategies whose metrics motivate the test—with per-feature timestamp audits ruling out label leakage before any evaluation. Evaluate firehose fill-rate and net expectancy (net of the runner's spread/slippage assumptions) against the current all-blocked baseline on a sealed holdout window of ≥300 signals spanning ≥8 weeks, with success predefined as fill-rate improving by ≥10 percentage points AND net expectancy > 0 with a bootstrap 95% CI excluding zero. Falsify—and keep the challenger unpromoted—if any leakage audit trips, either threshold is missed, or holdout expectancy decays more than a preregistered bound relative to in-sample. All writes remain confined to research artifacts (`bot/research/`, `bot/reports/research/`, staged `validated_states.json`); protected live YAML, runtime gating, and `intel/intelligent_champion.json` are untouched, with any promotion still gated by `promote.py`. If supported, expand the allowlist one cohort per subsequent cycle, each re-tested against fresh sealed data under these same gates.