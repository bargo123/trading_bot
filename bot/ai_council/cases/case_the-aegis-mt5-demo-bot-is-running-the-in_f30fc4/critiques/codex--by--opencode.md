The 1.5x threshold and 10% improvement target are arbitrary unless pre-registered and justified from training-only analysis, creating threshold-selection overfitting risk. “Measured” costs need a fixed, conservative methodology across regimes; otherwise net expectancy is not reproducible. A single sealed-holdout comparison lacks confidence intervals, significance testing, and stress validation for low-coverage or tail-loss periods.