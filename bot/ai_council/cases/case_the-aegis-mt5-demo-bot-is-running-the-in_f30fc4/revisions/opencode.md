Pre-register the ATR lookback, percentile, and minimum-trade-count candidates before testing, select them only through chronological walk-forward validation across multiple market regimes, then evaluate the chosen specification once on a sealed final holdout. Compare net expectancy and maximum drawdown over an explicitly defined common horizon, requiring a statistically supported expectancy improvement (bootstrap confidence interval excluding zero) while trade count remains within the pre-registered limit and drawdown does not materially worsen within its confidence interval. Apply existing spread and slippage assumptions to every evaluation and reject any configuration that fails a gate. Record parameters, splits, metrics, confidence intervals, and failures in `ExperimentRegistry`; keep all implementation in research-only configuration, without modifying immutable live YAML or bypassing champion-promotion gates.