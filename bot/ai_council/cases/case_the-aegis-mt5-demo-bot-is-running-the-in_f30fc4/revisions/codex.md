Next cycle should pre-register the abstention threshold and acceptance criteria, selecting and justifying them solely from training data with sensitivity analysis to limit threshold-selection overfitting. The rule should admit signals only when predicted edge exceeds costs estimated by a fixed, conservative methodology—specified before evaluation and applied consistently across regimes—including spread, slippage, and adverse execution assumptions. Evaluate it against the unchanged champion on the sealed holdout, reporting bootstrap confidence intervals and a pre-specified significance test for net expectancy, alongside maximum drawdown and coverage. Accept only if the improvement is statistically and practically credible, drawdown does not worsen, coverage remains at least 60%, and performance survives predefined stress tests emphasizing low-coverage periods and tail losses; otherwise reject it.