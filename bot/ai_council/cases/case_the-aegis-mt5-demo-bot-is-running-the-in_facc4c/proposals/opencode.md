**Proposal: Gate-validity audit — offline replay of fired-vs-skipped states.** Current evidence shows the validated-state gate is the sole binding constraint (firehose_throughput.json, 2026-08-22: 16,261/20,573 opportunities skipped, 100% with reason `state_not_in_validated_set`), yet the validated set is anchored to champion CORE_STRATEGY_V1 whose own record is net-negative (-0.027R IS / -0.019R OOS, PF 0.27) with a cosmetic 95.7% win rate (avg_win 0.031 vs avg_loss -2.57), so the gate may be filtering toward negative-expectancy states. Next cycle, run an offline replay experiment (registered in ExperimentRegistry) that scores forward net-of-cost expectancy — using the measured p75 costs already recorded per symbol, e.g. USDJPY 1.6 pips — for all logged fired states vs skipped states over fixed 1–10 bar horizons. Falsifiable prediction: if the gate adds value, fired-state expectancy exceeds skipped-state expectancy with the difference's bootstrap 95% CI excluding zero; if skipped states are equal or better, the validated set is mis-specified. In that failure case, re-mine candidate validated states from the reconciled exits' positive pockets (outcome_learning.md: USDCHF exp +0.014 n=350, USDCAD exp +0.005 n=337) instead of from CORE_STRATEGY_V1's state signature. This stays research-only — no code changes, no champion promotion, no live config touched.