Define a validated state as two completed, non-overlapping observation intervals meeting the existing state, quote-quality, and freshness gates; screen the pre-registered challenger through walk-forward cross-validation with lag-aware, state-transition slippage. Advance only one pre-specified candidate to the sealed holdout, evaluated once over a fixed horizon with block-bootstrap confidence intervals for net expectancy and drawdown. Accept only if net expectancy improvement is statistically supported, drawdown is non-inferior, and execution-adjusted trade participation remains within a pre-registered bound; report invalidation rates as descriptive selection effects, not an independent quality gate.