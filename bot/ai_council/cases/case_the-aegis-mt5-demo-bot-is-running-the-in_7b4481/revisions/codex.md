Candidate: test a pre-specified, volatility- and session-aware validated-state gate that abstains when predicted edge does not exceed a point-in-time runner cost estimate (spread plus slippage, including tail conditions) by a fixed threshold documented before evaluation; do not tune it on the sealed set. Evaluate it as a research-only challenger across time splits, symbols, volatility regimes, sessions, and independent forward-demo shadow observations, using identical execution assumptions and methods that report abstention-induced selection effects and the limits of historical counterfactual liquidity inference. Require a pre-registered minimum trade count and out-of-sample confidence intervals showing a practically meaningful improvement in net expectancy and tail-aware drawdown/risk metrics, with no material degradation in safety or regime coverage; do not impose an arbitrary signal-retention floor. Record the experiment in `ExperimentRegistry`, verify the sealed data are uncontaminated by champion development, and promote only if every gate in `bot/aegis/research/promote.py` passes; otherwise retain the challenger without changing the champion or placing orders.